import { Injectable } from '@angular/core';
import { BehaviorSubject } from 'rxjs';

@Injectable({
  providedIn: 'root'
})
export class PriceService {
  // price;
  //
  // public priceSource = new BehaviorSubject<string>('def message');
  // currentPrice = this.priceSource.asObservable();
  //
  // constructor() { }
  //
  // changePrice(message) {
  //   this.priceSource.next(message);
  // }

}
